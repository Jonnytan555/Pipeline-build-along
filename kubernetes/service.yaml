apiVersion: v1
kind: Service
metadata:
  name: pipeline-api
  namespace: pipeline
  labels:
    app: pipeline-api
spec:
  selector:
    app: pipeline-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP   # internal only; Ingress handles external traffic
