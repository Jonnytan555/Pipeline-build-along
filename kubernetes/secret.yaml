# DO NOT commit real values — use Sealed Secrets or AWS Secrets Manager in production.
# Generate values: echo -n 'value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: pipeline-secret
  namespace: pipeline
type: Opaque
data:
  DB_PASSWORD: cGlwZWxpbmVfcGFzcw==   # pipeline_pass (base64)
